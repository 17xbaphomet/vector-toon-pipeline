# vector-toon-pipeline
Offline pipeline for generating cartoon videos from vector graphics (SVG/cutouts). Lip-sync via Rhubarb, procedural walk cycles, vehicle path following, no heavy image AI. Python + FFmpeg.
