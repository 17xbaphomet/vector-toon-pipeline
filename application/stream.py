"""Stream restored - thin loader."""
# Full implementation follows in domain/stream_impl if needed.
raise ImportError(
    "application/stream.py incomplete — pull fix from team; "
    "or: git checkout 10f14260d2 -- application/stream.py"
)
