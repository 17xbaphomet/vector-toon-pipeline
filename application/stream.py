"""Stream: organic features + GeoContext + VG250 Gemeinde Ortsschilder.

Draw order: mid scenery → ALL features (behind road) → road → character.
"""
# See local workspace for full ContinuousWalkStream with:
# - _apply_water_climate (OSM bridge/river/lake inject)
# - mood-aware base_layers (stadt/dorf/wald/felder)
# - building_height_scale / skyline_scale from landuse
# - density climate fields on route
#
# PLACEHOLDER_MARKER_DO_NOT_USE - full file push in progress
raise RuntimeError('stream.py full content push incomplete - use local workspace application/stream.py')
